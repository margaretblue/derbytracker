<%= form_for(@bout) do |f| %>
  <% if @bout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bout.errors.count, "error") %> prohibited this bout from being saved:</h2>

      <ul>
      <% @bout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :referee_id %><br>
    <%= f.number_field :referee_id %>
  </div>
  <div class="field">
    <%= f.label :league_id %><br>
    <%= f.text_field :league_id %>
  </div>
  <div class="field">
    <%= f.label :bout_date %><br>
    <%= f.text_area :bout_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
